#pragma once


namespace ne::constants {

//Maximum iteration count (when finding pen)
inline constexpr int epaIterations = 32;

//Minimum escape tolerance (when finding pen)
inline constexpr float epaEpslion = 1e-5f;

} //namespace ne::constants
